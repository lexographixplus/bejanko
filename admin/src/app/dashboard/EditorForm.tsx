import { savePost } from '@/app/actions';
import type { Post } from '@/lib/content';

function bodyToText(html=''){return html.replace(/<br\s*\/?>/gi,'\n').replace(/<\/p>/gi,'\n\n').replace(/<[^>]+>/g,'').replace(/&lt;/g,'<').replace(/&gt;/g,'>').replace(/&quot;/g,'"').replace(/&#039;/g,"'").replace(/&amp;/g,'&').trim()}

export default function EditorForm({post}:{post?:Post}){
  return <form action={savePost} className="editor"><input type="hidden" name="id" value={post?.id||''}/><div className="formGrid"><div className="field"><label>Content type</label><select name="type" defaultValue={post?.type||'essay'}><option value="essay">Essay</option><option value="note">Note</option><option value="quote">Quote</option><option value="book">Book</option></select></div><div className="field"><label>Author</label><input name="author" defaultValue={post?.author||'B.E. Janko Jnr.'}/></div><div className="field full"><label>Title</label><input name="title" required defaultValue={post?.title||''}/></div><div className="field full"><label>Slug</label><input name="slug" defaultValue={post?.slug||''} placeholder="Generated from title when blank"/></div><div className="field full"><label>Excerpt</label><textarea name="excerpt" style={{minHeight:100}} defaultValue={post?.excerpt||''}/></div><div className="field full"><label>Body</label><textarea name="bodyText" defaultValue={bodyToText(post?.bodyHtml)}/></div><div className="field"><label>Cover image URL</label><input name="coverImage" defaultValue={post?.coverImage||''}/></div><div className="field"><label>Reading time (minutes)</label><input type="number" min="0" name="readingMinutes" defaultValue={post?.readingMinutes||0}/></div><div className="field full checks"><label><input type="checkbox" name="featured" defaultChecked={post?.featured}/> Featured</label><label><input type="checkbox" name="startHere" defaultChecked={post?.startHere}/> Start Here selection</label></div><div className="field full"><button className="btn primary" type="submit">Save {post?'changes':'draft'}</button></div></div></form>
}
